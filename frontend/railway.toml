[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules/.cache .next tsconfig.tsbuildinfo && npm ci --no-audit --no-fund && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
