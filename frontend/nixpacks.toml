providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --no-audit --no-fund"]

[phases.build]
cmds = ["rm -rf node_modules/.cache .next tsconfig.tsbuildinfo && npm run build"]

[start]
cmd = "npm start"